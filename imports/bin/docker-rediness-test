#!/bin/sh
#
# Readiness test
#
set -e

CONTAINER_STATUS=$( cat /tmp/container_status )
if [ "$CONTAINER_STATUS" != 'ready' ]; then
    exit 1;
fi

if [ -z "mysqladmin ping -h127.0.0.1" ]; then
    exit 1;
fi

sh -c "mysqladmin ping -h127.0.0.1"
exit $?